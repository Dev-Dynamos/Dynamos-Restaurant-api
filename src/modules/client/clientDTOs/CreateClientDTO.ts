export interface CreateClientDTO {
  nome: string;
  email: string;
  password: string;
  telefone: string;
}

export interface UpdateClientDTO {
  id: string;
  nome: string;
  email: string;
  password: string;
  telefone: string;
}
