import { AdminRepository } from "../application/adminRepository";
import { Admin } from "../core/admin";

export class AdminRepositoryAdapter implements AdminRepository {
  async createAdmin(props: Admin): Promise<void> {
    //////////////////////////////////////
  }
}
